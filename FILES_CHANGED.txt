Visual-replay canonical test fix (code-only delta)

CHANGED:
  site/index.html   deckById(id) now defensive: guards missing/non-array state.decks, returns null (predicate d=>d.id===id unchanged). youPlayerOf safe for missing state/decks/unknown id/no deck_id (returns neutral 1).
  Inkwell.dc.html   byte-identical mirror.
  tools/visual-replay.test.mjs   fixture state.decks:[]; +3 missing-state regression assertions. No existing assertion removed/weakened.

EXECUTED (sandbox JS, canonical logic): visual-replay 14/14 passed, 0 failed. Mirror byte-identical.
NOT EXECUTED (no runner): node tools/visual-replay.test.mjs, npm run test:all, Python, Playwright.
No generated data. No deploy/push.
