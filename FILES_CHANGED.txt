M1.6.1 — Player Trust Hotfix (code-only delta on da786668 + Visual Replay v2)

CHANGED:
  site/index.html   +cardByName() prototype method (fixes replay TypeError); +10 portfolio i18n keys + chrome keys (buy/wantOwn/type labels/owned/all/close) EN+PT; Advisor: removed duplicate deckStatus render, fixed have field, buy-list now uses canonical purchasesToUnlockNext + Shared Cards block (sharedConflicts).
  Inkwell.dc.html   byte-identical mirror.
  tools/i18n-contract.test.mjs   NEW — enumerates this.t() keys, fails on any EN/PT gap or raw-key render.
  tools/visual-replay.test.mjs   canonical (now runs; 11/11 with cardByName defined).
  package.json      +test:i18n, +test:replay, test:all includes both.

EXECUTED (sandbox JS): parse OK; i18n-contract PASS (252 keys, EN=PT=297, 0 raw); visual-replay 11/11.
NOT EXECUTED (no runner): Python unittest suites, validate_release.py --root site, Playwright smoke.
Decks sourced only from active user's state.decks. No generated data touched. No deploy/push.
