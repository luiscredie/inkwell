INKWELL — extract over repo root, then:

  npm run verify
  git add -A && git commit -m "Simplify deploy: push-triggered Pages, one verify command"
  git push

That is the whole flow now. Push deploys.

WHAT IS IN HERE

  .github/workflows/deploy-pages.yml   deploys on push to main (no longer waits
                                       for the checks workflow; old gate is
                                       commented out in the file if you want it
                                       back)
  package.json                         npm run verify = test:all + test:py + validate
                                       test:all raised from 14 to 16 suites so it
                                       now matches CI exactly
                                       legacy "test:js" removed (it covered 9 of 16
                                       and gave a misleading green)
  ROADMAP.md, github.md                records

WHY test:all GREW INSTEAD OF SHRINKING

You asked to simplify, not to check less. Before this, test:all ran 14 suites
while CI ran 16 — so a local pass could still be a broken build. Now that deploy
happens on push, your local run is the last thing that looks at the code before
players do. One command, full coverage, no second list to remember.

KEPT: the "Verify deploy root" step in the deploy workflow. One second, and it is
what stops a structurally broken root going live as a 404.

REMEMBER: restore the gate in deploy-pages.yml before outside people use the site.
Right now a push with a regression is live in seconds.
