CORREÇÃO — build:web falhava: "No matching export for lerDecksSalvos"

  sim-project/web/inkwell.ts   ->  copie para  lorcana-sim/web/inkwell.ts

Depois:

  cd "$HOME\OneDrive\Desktop\lorcana-sim"
  npm.cmd run build:web
  Select-String -Path ".\web\game.js" -Pattern "DISCIPLINE AND STRENGTH" -SimpleMatch -Quiet

Se der True:

  Copy-Item ".\web\game.js" "$HOME\OneDrive\Desktop\inkwell\site\sim\game.js" -Force
  cd "$HOME\OneDrive\Desktop\inkwell"
  git add -A; git commit -m "Bundle do motor 2.0"; git push

════════════════════════════════════════════════════════════
ERRO MEU, E VALE ENTENDER O TIPO

Eu montei o pacote do motor 2.0 a partir do PRIMEIRO zip que você me mandou do
simulador. Depois disso o projeto evoluiu: ganhou `lerDecksSalvos`, a função que lê
os decks salvos do site. O app.ts atual importa ela.

Quando meu inkwell.ts sobrescreveu o seu, essa função sumiu — e o esbuild parou
exatamente onde devia parar.

Não foi um erro de código: foi de PROCEDÊNCIA. Entreguei "arquivos completos" para
facilitar a aplicação, mas arquivo completo carrega tudo que estava na versão de
origem, inclusive a ausência do que veio depois. Para os 10 arquivos do motor isso
não deu problema, porque nenhum tinha mudado. Este tinha.

Este arquivo é o SEU atual, com a única linha que eu precisava mudar (o caminho da
arte) já aplicada. Nada mais.

Exports conferidos: carregarCatalogo, listasDaUrl, urlDaImagem, lerDecksSalvos.

════════════════════════════════════════════════════════════
DEPOIS QUE SUBIR

O contador no topo de /sim/game.html tem de passar de 40. Se passar, as 7 cartas
estão valendo e seu deck deve destravar — menos Lumpy - Hunny Druid (LOR13-42),
que entrou no deck depois e ainda não tem implementação.
